# Tox (http://tox.testrun.org/) is a tool for running tests
# Install:
#   pip install tox
# Run:
#   tox

[tox]
envlist =
    py310
    py311
    py312
    py313
    py314

[testenv]
commands =
    python unitest.py
    #flake8 --exclude=build,.tox,.git
